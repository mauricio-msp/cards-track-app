services:
  postgres:
    image: postgres:17
    container_name: cardtrack_db
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: cardtrack
    ports:
      - '5432:5432'